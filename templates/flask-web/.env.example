# Copy to .env (ignored by git). Never commit real values.
FLASK_SECRET_KEY=change-me
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/__PROJECT_NAME__
# External data sources, one per entry in project/profile.yaml, named DS_<NAME>_URL
